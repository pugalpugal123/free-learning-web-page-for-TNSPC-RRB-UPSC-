{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Run Project (index.html)",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}